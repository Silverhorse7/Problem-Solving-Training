#include <bits/stdc++.h>

using namespace std;


signed main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    //white at (1,1) , black at (1,n)
    /*
     * |W
     * |
     * |    B
     */
    int n;
    cin>>n;
    if(n%2)cout<<"black";
    else
        cout<<"white"<<endl<<"1 2"<<endl;
}